{
  "name": "@lucifer/core",
  "version": "0.1.0",
  "description": "lucifer-research core types, router, security and pipeline",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --ext .ts",
    "dev": "tsc --watch",
    "prepare": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^3.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
